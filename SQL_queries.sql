
--SQL QUERIES
-- Select the city list
SELECT * FROM city_list

-- select only the cities that interests me
SELECT * FROM city_data
WHERE city = 'San Francisco';

SELECT * FROM city_data
WHERE city = 'Guangzhou';

SELECT * FROM city_data
WHERE city = 'Ljubljana';

SELECT * FROM city_data
WHERE city = 'Helsinki';

-- select all data form global_data
SELECT * FROM global_data